const path = require("path");

module.exports = {
  config: path.resolve(__dirname, "server", "database", "config", "database.js"),
  "migrations-path": path.resolve(__dirname, "server", "database", "migrations"),
};
